# Voyage Analytics — project requirements



pandas==2.2.2
numpy==1.26.4
scipy==1.15.2
matplotlib==3.9.2
seaborn==0.13.2
plotly==6.1.0
missingno==0.5.2

# ---- Notebooks ----
jupyter==1.1.1
notebook==7.2.2
ipykernel==6.29.4
nbformat==5.10.4

scikit-learn==1.6.1
xgboost==2.1.1
lightgbm==4.5.0

mlflow==2.16.2
fastapi==0.115.0
uvicorn==0.30.6
flask==3.0.3
streamlit==1.38.0
pydantic==2.9.2
joblib==1.4.2

# ---- Data quality / validation ----
# Validation is hand-rolled in src/validation (no third-party dependency).
#
# great-expectations is intentionally NOT pinned here. It was listed during
# planning as the eventual replacement for the hand-rolled checks, but it is
# never imported, and version 1.0.5 requires pandas<2.2 — which conflicts with
# the pandas 2.2.2 the models were fitted against. Installing it would force a
# pandas downgrade purely for a package the project does not use.
#
# When the checks are migrated to Great Expectations, install it in a separate
# environment (or use a release compatible with pandas>=2.2) and retrain, rather
# than downgrading pandas here:
#     pip install "great-expectations>=1.1"

# ---- Utilities ----
python-dotenv==1.0.1
pyyaml==6.0.2

# ---- Storage ----
pyarrow==17.0.0
